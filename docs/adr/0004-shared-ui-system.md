# Share one UI system across public and admin

The Public UI surface and Admin UI surface will use one Shared UI system. Admin exists for content maintenance and does not need a separate visual vocabulary or class set. TipTap specialized controls remain an explicit editor-owned exception; they may consume the Shared UI system's tokens without being forced into its generic components.
