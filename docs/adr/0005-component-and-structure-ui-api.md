# Use React components for controls and semantic CSS for structures

Interactive UI elements will be implemented as reusable React components with explicit variants. Visual UI structures will be represented by semantic CSS classes, keeping layout and presentation deterministic without creating classes for individual content blocks. This separates behavior from composition while preserving the distinct Public UI surface, Admin UI surface, and TipTap specialized controls.
